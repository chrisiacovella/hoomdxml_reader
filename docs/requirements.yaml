name: hoomdxml_reader-docs
channels: 
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - networkx
  - numpydoc
  - sphinx_rtd_theme
  - gsd

    # Pip-only installs
  #- pip:

